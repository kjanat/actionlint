<a id="hello"></a>
## Hello

Example input:

```yaml
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ unknown }}
```

Output:

```console
This section will be generated
```

[Playground](URL_WILL_BE_GENERATED)

The output code block carries a language identifier.
