{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/kjanat/actionlint/HEAD/actionlint.schema.json",
  "$comment": "Generated from config.go by go generate -run generate-config-schema. DO NOT EDIT.",
  "properties": {
    "tools": {
      "oneOf": [
        {
          "properties": {
            "shellcheck": {
              "oneOf": [
                {
                  "oneOf": [
                    {
                      "type": "boolean"
                    },
                    {
                      "properties": {
                        "enabled": {
                          "oneOf": [
                            {
                              "type": "boolean",
                              "default": true
                            },
                            {
                              "type": "null"
                            }
                          ],
                          "title": "enabled",
                          "description": "Enable ShellCheck analysis. Omission or null keeps it enabled.",
                          "markdownDescription": "Enable ShellCheck analysis. Omission or null keeps it enabled."
                        },
                        "config": {
                          "oneOf": [
                            {
                              "oneOf": [
                                {
                                  "type": "string",
                                  "minLength": 1
                                },
                                {
                                  "properties": {
                                    "disable": {
                                      "oneOf": [
                                        {
                                          "items": {
                                            "type": "string",
                                            "pattern": "^(all|(SC)?[0-9]+(-(SC)?[0-9]+)?)$"
                                          },
                                          "type": "array"
                                        },
                                        {
                                          "type": "null"
                                        }
                                      ],
                                      "title": "disable",
                                      "description": "Lists codes, code ranges or all, for example [SC2086, SC3000-SC4000].",
                                      "markdownDescription": "Lists codes, code ranges or all, for example [SC2086, SC3000-SC4000]."
                                    },
                                    "enable": {
                                      "oneOf": [
                                        {
                                          "items": {
                                            "type": "string",
                                            "pattern": "^[a-zA-Z-]+$"
                                          },
                                          "type": "array"
                                        },
                                        {
                                          "type": "null"
                                        }
                                      ],
                                      "title": "enable",
                                      "description": "Lists optional check names, or all. ShellCheck validates available names.",
                                      "markdownDescription": "Lists optional check names, or all. ShellCheck validates available names."
                                    },
                                    "shell": {
                                      "oneOf": [
                                        {
                                          "type": "string",
                                          "enum": [
                                            "sh",
                                            "bash",
                                            "dash",
                                            "ksh",
                                            "busybox"
                                          ]
                                        },
                                        {
                                          "type": "null"
                                        }
                                      ],
                                      "title": "shell",
                                      "description": "Overrides the inferred dialect for shell scripts, not Python or PowerShell steps.",
                                      "markdownDescription": "Overrides the inferred dialect for shell scripts, not Python or PowerShell steps."
                                    },
                                    "extended-analysis": {
                                      "oneOf": [
                                        {
                                          "type": "boolean"
                                        },
                                        {
                                          "type": "null"
                                        }
                                      ],
                                      "title": "extended-analysis",
                                      "description": "Enables or disables ShellCheck dataflow analysis. Omission keeps its default.",
                                      "markdownDescription": "Enables or disables ShellCheck dataflow analysis. Omission keeps its default."
                                    },
                                    "external-sources": {
                                      "oneOf": [
                                        {
                                          "type": "boolean"
                                        },
                                        {
                                          "type": "null"
                                        }
                                      ],
                                      "title": "external-sources",
                                      "description": "Controls following sourced files. Omission preserves actionlint's enabled default.",
                                      "markdownDescription": "Controls following sourced files. Omission preserves actionlint's enabled default."
                                    },
                                    "source-path": {
                                      "oneOf": [
                                        {
                                          "items": {
                                            "type": "string",
                                            "minLength": 1
                                          },
                                          "type": "array"
                                        },
                                        {
                                          "type": "null"
                                        }
                                      ],
                                      "title": "source-path",
                                      "description": "Lists directories searched for sourced files, relative to the run step's effective working directory. Scripts arrive on stdin, so SCRIPTDIR does not denote the workflow file's directory.",
                                      "markdownDescription": "Lists directories searched for sourced files, relative to the run step's effective working directory. Scripts arrive on stdin, so SCRIPTDIR does not denote the workflow file's directory."
                                    }
                                  },
                                  "additionalProperties": false,
                                  "type": "object",
                                  "description": "Is a typed representation of project-wide ShellCheck directives.",
                                  "markdownDescription": "Is a typed representation of project-wide ShellCheck directives."
                                }
                              ]
                            },
                            {
                              "type": "null"
                            }
                          ],
                          "title": "config",
                          "description": "Inline directives or an rc file/directory. Relative paths and {configdir} use the directory containing actionlint.yaml or actionlint.yml; {gitdir} selects the repository root.",
                          "markdownDescription": "Inline directives or an rc file/directory. Relative paths and {configdir} use the directory containing actionlint.yaml or actionlint.yml; {gitdir} selects the repository root."
                        }
                      },
                      "additionalProperties": false,
                      "type": "object"
                    }
                  ]
                },
                {
                  "type": "null"
                }
              ],
              "title": "shellcheck",
              "description": "Configures checking of embedded shell scripts.",
              "markdownDescription": "Configures checking of embedded shell scripts."
            }
          },
          "additionalProperties": false,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "title": "tools",
      "description": "Configures external linters for both the CLI and GitHub Action.",
      "markdownDescription": "Configures external linters for both the CLI and GitHub Action."
    },
    "self-hosted-runner": {
      "oneOf": [
        {
          "properties": {
            "labels": {
              "oneOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "title": "labels",
              "description": "Lists additional self-hosted runner labels accepted in `runs-on`.\n\nFor example, `[linux.2xlarge, custom-*]` accepts that label and matching custom labels. Patterns use Go `path.Match` syntax. Omit this key, use `null`, or use `[]` to add no labels.",
              "markdownDescription": "Lists additional self-hosted runner labels accepted in `runs-on`.\n\nFor example, `[linux.2xlarge, custom-*]` accepts that label and matching custom labels. Patterns use Go `path.Match` syntax. Omit this key, use `null`, or use `[]` to add no labels."
            }
          },
          "additionalProperties": false,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "title": "self-hosted-runner",
      "description": "Configures extra labels accepted for self-hosted runners.\n\nAdd your labels under `labels`, for example `{labels: [linux.2xlarge]}`.",
      "markdownDescription": "Configures extra labels accepted for self-hosted runners.\n\nAdd your labels under `labels`, for example `{labels: [linux.2xlarge]}`."
    },
    "config-variables": {
      "oneOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "title": "config-variables",
      "description": "Lists configuration variable names available to the checked workflows through `vars`.\n\nOmit this key or use `null` to disable variable-name checking. Use `[]` to allow no variables. A list such as `[DEFAULT_RUNNER, ENVIRONMENT_STAGE]` reports names outside that list as undefined.",
      "markdownDescription": "Lists configuration variable names available to the checked workflows through `vars`.\n\nOmit this key or use `null` to disable variable-name checking. Use `[]` to allow no variables. A list such as `[DEFAULT_RUNNER, ENVIRONMENT_STAGE]` reports names outside that list as undefined."
    },
    "config-secrets": {
      "oneOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "title": "config-secrets",
      "description": "Lists secret names available to the checked workflows through `secrets`.\n\nOmit this key or use `null` to disable secret-name checking. Use `[]` to allow only built-in secrets and secrets declared in `on.workflow_call.secrets`. A list such as `[DEPLOY_TOKEN, API_KEY]` also allows those names; other names are reported as undefined. Matching is case-insensitive.\n\n`GITHUB_TOKEN`, `ACTIONS_STEP_DEBUG`, and `ACTIONS_RUNNER_DEBUG` are always allowed. List names only, never secret values.",
      "markdownDescription": "Lists secret names available to the checked workflows through `secrets`.\n\nOmit this key or use `null` to disable secret-name checking. Use `[]` to allow only built-in secrets and secrets declared in `on.workflow_call.secrets`. A list such as `[DEPLOY_TOKEN, API_KEY]` also allows those names; other names are reported as undefined. Matching is case-insensitive.\n\n`GITHUB_TOKEN`, `ACTIONS_STEP_DEBUG`, and `ACTIONS_RUNNER_DEBUG` are always allowed. List names only, never secret values."
    },
    "paths": {
      "oneOf": [
        {
          "additionalProperties": {
            "properties": {
              "ignore": {
                "oneOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "ignore",
                "description": "Suppresses diagnostics whose message matches any of these Go regular expressions.\n\nApplies only to workflow files matching the parent path glob. For example, `[\"shellcheck reported issue in this script: SC2086\"]` ignores that ShellCheck diagnostic. Omit this key, use `null`, or use `[]` to suppress nothing. Like the `-ignore` CLI option.",
                "markdownDescription": "Suppresses diagnostics whose message matches any of these Go regular expressions.\n\nApplies only to workflow files matching the parent path glob. For example, `[\"shellcheck reported issue in this script: SC2086\"]` ignores that ShellCheck diagnostic. Omit this key, use `null`, or use `[]` to suppress nothing. Like the `-ignore` CLI option."
              }
            },
            "additionalProperties": false,
            "type": "object",
            "description": "Is a configuration for specific file path pattern. This is for values of the \"paths\" mapping in the configuration file.",
            "markdownDescription": "Is a configuration for specific file path pattern. This is for values of the \"paths\" mapping in the configuration file."
          },
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "title": "paths",
      "description": "Applies configuration to workflow files matching a glob pattern.\n\nKeys are paths relative to the repository root, using `/` separators and doublestar glob syntax, for example `.github/workflows/**/*.yaml`. All matching entries apply. Each entry can set `ignore`.",
      "markdownDescription": "Applies configuration to workflow files matching a glob pattern.\n\nKeys are paths relative to the repository root, using `/` separators and doublestar glob syntax, for example `.github/workflows/**/*.yaml`. All matching entries apply. Each entry can set `ignore`."
    },
    "assume-default-permissions": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "restricted",
            "permissive"
          ],
          "default": "restricted"
        },
        {
          "type": "null"
        }
      ],
      "title": "assume-default-permissions",
      "description": "Selects the repository's assumed default token permissions when checking reusable workflow calls whose calling job and workflow both omit `permissions:`.\n\n`restricted` (the default) grants read access to `contents` and `packages` only. `permissive` assumes read/write access, except `id-token`, which still requires an explicit grant. Omit this key or use `null` to assume `restricted`.",
      "markdownDescription": "Selects the repository's assumed default token permissions when checking reusable workflow calls whose calling job and workflow both omit `permissions:`.\n\n`restricted` (the default) grants read access to `contents` and `packages` only. `permissive` assumes read/write access, except `id-token`, which still requires an explicit grant. Omit this key or use `null` to assume `restricted`."
    },
    "policy": {
      "oneOf": [
        {
          "properties": {
            "cache-call-unrestricted": {
              "oneOf": [
                {
                  "type": "boolean",
                  "default": true
                },
                {
                  "type": "null"
                }
              ],
              "title": "cache-call-unrestricted",
              "description": "Requires explicit cache ceilings on low-trust reusable calls. Enabled by default. Set false to disable it; null or omission keeps the default.",
              "markdownDescription": "Requires explicit cache ceilings on low-trust reusable calls. Enabled by default. Set false to disable it; null or omission keeps the default."
            },
            "cache-operation": {
              "oneOf": [
                {
                  "type": "boolean",
                  "default": true
                },
                {
                  "type": "null"
                }
              ],
              "title": "cache-operation",
              "description": "Reports official cache action steps disabled by an explicit cache mode. Enabled by default. Set false to disable it; null or omission keeps the default.",
              "markdownDescription": "Reports official cache action steps disabled by an explicit cache mode. Enabled by default. Set false to disable it; null or omission keeps the default."
            },
            "cache-write-untrusted": {
              "oneOf": [
                {
                  "type": "boolean",
                  "default": true
                },
                {
                  "type": "null"
                }
              ],
              "title": "cache-write-untrusted",
              "description": "Reports write-capable cache modes on low-trust triggers. Enabled by default. Set false to disable it; null or omission keeps the default.",
              "markdownDescription": "Reports write-capable cache modes on low-trust triggers. Enabled by default. Set false to disable it; null or omission keeps the default."
            },
            "disallow-suppressions": {
              "oneOf": [
                {
                  "oneOf": [
                    {
                      "type": "boolean"
                    },
                    {
                      "properties": {
                        "rules": {
                          "items": {
                            "type": "string",
                            "enum": [
                              "cache-call-unrestricted",
                              "cache-operation",
                              "cache-write-untrusted"
                            ]
                          },
                          "type": "array",
                          "minItems": 1,
                          "title": "rules",
                          "description": "Rule IDs whose inline exceptions are prohibited. Omit to select all suppressible rules.",
                          "markdownDescription": "Rule IDs whose inline exceptions are prohibited. Omit to select all suppressible rules."
                        },
                        "report": {
                          "type": "string",
                          "enum": [
                            "suppression",
                            "violation",
                            "all"
                          ],
                          "title": "report",
                          "description": "Report the prohibited directive or retain original violations or both.",
                          "default": "all",
                          "markdownDescription": "Report the prohibited directive or retain original violations or both."
                        }
                      },
                      "additionalProperties": false,
                      "type": "object"
                    }
                  ]
                },
                {
                  "type": "null"
                }
              ],
              "title": "disallow-suppressions",
              "description": "Restricts inline cache policy exceptions. `true` or `{}` reports each prohibited directive and retains its original violations. Omission, null, or false permits exceptions. Both ignore and ignore-next-line are covered equally.\n\nUse `{rules: [cache-call-unrestricted], report: all}` to restrict selected rule IDs. Omitted rules selects all suppressible rules; an explicit list must be nonempty. Report accepts suppression (directive only), violation (original findings only), or all (both kinds of diagnostic).",
              "markdownDescription": "Restricts inline cache policy exceptions. `true` or `{}` reports each prohibited directive and retains its original violations. Omission, null, or false permits exceptions. Both ignore and ignore-next-line are covered equally.\n\nUse `{rules: [cache-call-unrestricted], report: all}` to restrict selected rule IDs. Omitted rules selects all suppressible rules; an explicit list must be nonempty. Report accepts suppression (directive only), violation (original findings only), or all (both kinds of diagnostic)."
            },
            "require-commit-hash": {
              "oneOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "null"
                }
              ],
              "title": "require-commit-hash",
              "description": "Requires `uses:` references to be pinned to a full commit SHA, or an image digest for Docker images, when set to `true`.\n\nSet `false` to disable the check. Omit this key or use `null` to leave it unset; the check is disabled by default. Local references and references built with expressions are skipped.",
              "markdownDescription": "Requires `uses:` references to be pinned to a full commit SHA, or an image digest for Docker images, when set to `true`.\n\nSet `false` to disable the check. Omit this key or use `null` to leave it unset; the check is disabled by default. Local references and references built with expressions are skipped."
            },
            "require-job-timeout": {
              "oneOf": [
                {
                  "oneOf": [
                    {
                      "type": "boolean"
                    },
                    {
                      "properties": {
                        "min-minutes": {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "title": "min-minutes",
                          "description": "Smallest allowed job timeout in minutes. Must not exceed max-minutes.",
                          "markdownDescription": "Smallest allowed job timeout in minutes. Must not exceed max-minutes."
                        },
                        "max-minutes": {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "title": "max-minutes",
                          "description": "Largest allowed job timeout in minutes.",
                          "markdownDescription": "Largest allowed job timeout in minutes."
                        }
                      },
                      "additionalProperties": false,
                      "type": "object"
                    }
                  ]
                },
                {
                  "type": "null"
                }
              ],
              "title": "require-job-timeout",
              "description": "Requires jobs to declare `timeout-minutes` when set to `true`.\n\nUse `{min-minutes: 5, max-minutes: 60}` to require a timeout between 5 and 60 minutes. Either bound may be omitted. Bounds must be finite and greater than zero, and the minimum must not exceed the maximum. `{}` requires the key without bounds. Reusable workflow calls are skipped.\n\nSet `false` to disable the check. Omit this key or use `null` to leave it unset; the check is disabled by default.",
              "markdownDescription": "Requires jobs to declare `timeout-minutes` when set to `true`.\n\nUse `{min-minutes: 5, max-minutes: 60}` to require a timeout between 5 and 60 minutes. Either bound may be omitted. Bounds must be finite and greater than zero, and the minimum must not exceed the maximum. `{}` requires the key without bounds. Reusable workflow calls are skipped.\n\nSet `false` to disable the check. Omit this key or use `null` to leave it unset; the check is disabled by default."
            },
            "require-permissions": {
              "oneOf": [
                {
                  "oneOf": [
                    {
                      "type": "boolean"
                    },
                    {
                      "properties": {
                        "scope": {
                          "type": "string",
                          "enum": [
                            "workflow",
                            "job"
                          ],
                          "title": "scope",
                          "description": "Require a workflow-level declaration or a declaration on every job.",
                          "default": "workflow",
                          "markdownDescription": "Require a workflow-level declaration or a declaration on every job."
                        }
                      },
                      "additionalProperties": false,
                      "type": "object"
                    }
                  ]
                },
                {
                  "type": "null"
                }
              ],
              "title": "require-permissions",
              "description": "Requires an explicit `permissions:` declaration.\n\n`true` or `{scope: workflow}` requires a workflow-level declaration, including `permissions: {}`. `{scope: job}` requires a declaration on every job, including reusable workflow calls. `{}` enables workflow scope. This checks presence only; it does not infer the scopes a job needs.\n\nSet `false` to disable the check. Omit this key or use `null` to leave it unset; it is disabled by default.",
              "markdownDescription": "Requires an explicit `permissions:` declaration.\n\n`true` or `{scope: workflow}` requires a workflow-level declaration, including `permissions: {}`. `{scope: job}` requires a declaration on every job, including reusable workflow calls. `{}` enables workflow scope. This checks presence only; it does not infer the scopes a job needs.\n\nSet `false` to disable the check. Omit this key or use `null` to leave it unset; it is disabled by default."
            },
            "required-actions": {
              "oneOf": [
                {
                  "items": {
                    "type": "string",
                    "minLength": 1
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "title": "required-actions",
              "description": "Lists actions that every workflow must use in its steps.\n\nWrite entries like `uses:` values: `actions/checkout` accepts any ref, while `actions/checkout@v4*` also matches the ref. Both halves support glob patterns; `*` does not match `/`. Names are matched case-insensitively and refs case-sensitively.\n\nUse `[]` to disable the check. Omit this key or use `null` to leave it unset; no actions are required by default. Actions inside composite actions or called workflows are not counted.",
              "markdownDescription": "Lists actions that every workflow must use in its steps.\n\nWrite entries like `uses:` values: `actions/checkout` accepts any ref, while `actions/checkout@v4*` also matches the ref. Both halves support glob patterns; `*` does not match `/`. Names are matched case-insensitively and refs case-sensitively.\n\nUse `[]` to disable the check. Omit this key or use `null` to leave it unset; no actions are required by default. Actions inside composite actions or called workflows are not counted."
            }
          },
          "additionalProperties": false,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "title": "policy",
      "description": "Configures cache safety checks and repository conventions, such as pinned actions and job timeouts.\n\nCache policies default to true; other policies are opt-in. Set individual keys to override their defaults. Omit the mapping or use `{}` or `null` to keep defaults. Syntax checks always run.",
      "markdownDescription": "Configures cache safety checks and repository conventions, such as pinned actions and job timeouts.\n\nCache policies default to true; other policies are opt-in. Set individual keys to override their defaults. Omit the mapping or use `{}` or `null` to keep defaults. Syntax checks always run."
    }
  },
  "additionalProperties": false,
  "type": "object",
  "title": "actionlint configuration",
  "description": "Configures validation of GitHub Actions workflows for this repository.\n\nDeclare custom runner labels and available variable or secret names, suppress selected diagnostics by file path, choose assumed token permissions, and enable repository policy checks. Save as `.github/actionlint.yaml` or `.github/actionlint.yml`, or select a file with `-config-file`. Every setting is optional; normal workflow correctness checks run without a configuration file.",
  "markdownDescription": "Configures validation of GitHub Actions workflows for this repository.\n\nDeclare custom runner labels and available variable or secret names, suppress selected diagnostics by file path, choose assumed token permissions, and enable repository policy checks. Save as `.github/actionlint.yaml` or `.github/actionlint.yml`, or select a file with `-config-file`. Every setting is optional; normal workflow correctness checks run without a configuration file."
}
