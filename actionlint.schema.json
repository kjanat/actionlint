{
  "$id": "https://raw.githubusercontent.com/kjanat/actionlint/HEAD/actionlint.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Generated from config.go by go generate -run generate-config-schema. DO NOT EDIT.",
  "title": "actionlint configuration",
  "description": "Config is configuration of actionlint.",
  "type": "object",
  "properties": {
    "assume-default-permissions": {
      "oneOf": [
        {
          "description": "AssumeDefaultPermissions selects which repository \"Workflow permissions\" setting actionlint assumes\nwhen a workflow call's calling job declares no \"permissions:\" and neither does its workflow. The zero\nvalue means the key was not set, which is equivalent to DefaultPermissionsAssumptionRestricted.",
          "default": "restricted",
          "type": "string",
          "enum": [
            "restricted",
            "permissive"
          ]
        },
        {
          "type": "null"
        }
      ]
    },
    "config-secrets": {
      "oneOf": [
        {
          "description": "ConfigSecrets is names of secrets used in the checked workflows. When this value is nil, property\nnames of `secrets` context will not be checked. Otherwise actionlint will report a name which is\nnot listed here as an undefined secret.\nhttps://docs.github.com/en/actions/security-guides/using-secrets-in-github-actions",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "null"
        }
      ]
    },
    "config-variables": {
      "oneOf": [
        {
          "description": "ConfigVariables is names of configuration variables used in the checked workflows. When this value is nil,\nproperty names of `vars` context will not be checked. Otherwise actionlint will report a name which is not\nlisted here as undefined config variables.\nhttps://docs.github.com/en/actions/learn-github-actions/variables",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "null"
        }
      ]
    },
    "paths": {
      "oneOf": [
        {
          "description": "Paths is a \"paths\" mapping in the configuration file. The keys are glob patterns to match file paths.\nAnd the values are corresponding configurations applied to the file paths.",
          "type": "object",
          "additionalProperties": {
            "description": "PathConfig is a configuration for specific file path pattern.",
            "type": "object",
            "properties": {
              "ignore": {
                "oneOf": [
                  {
                    "description": "Ignore is a list of patterns. They are used for ignoring errors by matching to the error messages.\nIt is similar to the \"-ignore\" command line option.",
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  },
                  {
                    "type": "null"
                  }
                ]
              }
            },
            "additionalProperties": false
          }
        },
        {
          "type": "null"
        }
      ]
    },
    "policy": {
      "oneOf": [
        {
          "description": "Policy is a \"policy\" mapping in the configuration file. It turns on the checks which enforce the\nconventions chosen by the repository.",
          "type": "object",
          "properties": {
            "require-commit-hash": {
              "oneOf": [
                {
                  "description": "RequireCommitHash requires every \"uses:\" to be pinned to a full commit SHA, or to an image digest\nwhen it names a Docker image. Nil means the key was not set.",
                  "type": "boolean"
                },
                {
                  "type": "null"
                }
              ]
            },
            "require-job-timeout": {
              "oneOf": [
                {
                  "description": "RequireJobTimeout requires every job to set \"timeout-minutes\". Nil means the key was not set.",
                  "oneOf": [
                    {
                      "type": "boolean"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "max-minutes": {
                          "description": "Largest allowed job timeout in minutes.",
                          "type": "number",
                          "exclusiveMinimum": 0
                        }
                      },
                      "additionalProperties": false
                    }
                  ]
                },
                {
                  "type": "null"
                }
              ]
            },
            "required-actions": {
              "oneOf": [
                {
                  "description": "RequiredActions is the actions every workflow must use. Each entry is written like a \"uses:\"\nvalue and both of its halves are glob patterns. Nil means the key was not set. An empty non-nil\nvalue requires no action, which disables the check.",
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "type": "null"
        }
      ]
    },
    "self-hosted-runner": {
      "oneOf": [
        {
          "description": "SelfHostedRunner is configuration for self-hosted runner.",
          "type": "object",
          "properties": {
            "labels": {
              "oneOf": [
                {
                  "description": "Labels is label names for self-hosted runner.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false
}
