{
  "$id": "https://github.com/kjanat/actionlint/blob/HEAD/distribution/npm/targets.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$defs": {
    "target": {
      "description": "One published platform package.",
      "type": "object",
      "required": [
        "asset",
        "cpu",
        "format",
        "os",
        "pkg"
      ],
      "properties": {
        "asset": {
          "description": "GOOS_GOARCH pair as it appears in the release asset name, e.g. \"linux_amd64\". ARM builds carry the variant, e.g. \"linux_armv6\".",
          "type": "string",
          "pattern": "^[a-z0-9]+_[a-z0-9]+$"
        },
        "cpu": {
          "description": "Node process.arch value, used as the package's \"cpu\" field.",
          "enum": [
            "arm",
            "arm64",
            "ia32",
            "loong64",
            "mips",
            "mipsel",
            "ppc64",
            "riscv64",
            "s390x",
            "x64"
          ]
        },
        "format": {
          "description": "Archive format GoReleaser publishes for this platform.",
          "enum": [
            "tar.gz",
            "zip"
          ]
        },
        "os": {
          "description": "Node process.platform value, used as the package's \"os\" field.",
          "enum": [
            "aix",
            "darwin",
            "freebsd",
            "linux",
            "openbsd",
            "sunos",
            "win32"
          ]
        },
        "pkg": {
          "description": "Platform suffix of the package name, <platformScope>/<binary>-<pkg>. Must equal \"<os>-<cpu>\": the launcher derives the package name that way, so any other value publishes a package it can never resolve.",
          "type": "string",
          "pattern": "^[a-z0-9]+-[a-z0-9]+$"
        },
        "tier": {
          "description": "1 for platforms covered by CI, 2 for best-effort builds.",
          "type": "integer",
          "minimum": 1,
          "maximum": 3
        }
      },
      "additionalProperties": false
    }
  },
  "title": "npm distribution targets",
  "description": "Platforms actionlint publishes a prebuilt npm package for, and the GoReleaser release asset each one is cut from.",
  "type": "object",
  "required": [
    "binary",
    "facade",
    "platformScope",
    "targets"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "binary": {
      "description": "Executable inside each release archive, without any .exe suffix.",
      "type": "string",
      "pattern": "^[a-zA-Z0-9_-]+$"
    },
    "facade": {
      "description": "npm name of the user-facing package.",
      "type": "string",
      "pattern": "^(@[a-z0-9][a-z0-9-]*/)?[a-z0-9][a-z0-9._-]*$"
    },
    "platformScope": {
      "description": "npm scope the platform packages are published under, so they are named \"<platformScope>/<binary>-<pkg>\". A separate scope keeps eleven binary-only packages out of the facade's namespace.",
      "type": "string",
      "pattern": "^@[a-z0-9][a-z0-9-]*$"
    },
    "targets": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/target"
      }
    }
  },
  "additionalProperties": false
}
