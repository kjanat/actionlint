on:
  image_version:
    # A scalar image name is valid
    names: MyImage
    # Empty string is not allowed
    versions: ['']
    # Unknown key
    entrypoint: run.sh

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
