on:
  workflow_call:

jobs:
  comment:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - run: echo comment
  attest:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - run: echo attest
