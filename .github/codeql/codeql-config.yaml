## yaml-language-server: $schema=https://raw.githubusercontent.com/github/codeql-action/HEAD/src/db-config-schema.json
---
name: "CodeQL config"
paths-ignore: [testdata]
threat-models: [local]
query-filters:
  # The CLI intentionally reads and inspects paths selected by its caller.
  - exclude:
      id: go/path-injection
