---
name: CI
on:
  push: { branches: [master] }
  pull_request: null
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
permissions: { contents: read }
defaults: { run: { shell: "bash --noprofile --norc -exo pipefail {0}" } }
jobs:
  nix: { uses: $/.github/workflows/nix.yml }
  unit-tests:
    name: Unit tests
    strategy:
      fail-fast: false
      matrix: { os: [ubuntu-26.04, ubuntu-26.04-arm, macos-latest, macos-26-intel, windows-2025-vs2026, windows-11-vs2026-arm] }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: &ActionsCheckout actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with: &NoCreds { persist-credentials: false }
      - parallel:
          - uses: &SetupGo actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
            with: &GoMod { go-version-file: go.mod }
          - uses: &SetupUv astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
            with: &NoCache { enable-cache: false }
      - name: Install pyflakes
        run: &InstallPyflakes uv tool install pyflakes; pyflakes --version
      - &InstallShellCheck { name: Install ShellCheck, uses: $/.github/actions/setup-shellcheck }
      - name: Install shells for completion tests on Linux
        if: ${{ runner.os == 'Linux' }}
        run: |
          sudo apt-get update && sudo apt-get install -y --no-install-recommends zsh fish bash-completion
          zsh --version; fish --version
      - run: go test -v ./...
      # Check build without CGO
      - run: go build ./cmd/actionlint
        env: { CGO_ENABLED: 0 }
  race:
    name: Race detector and coverage
    runs-on: ubuntu-latest
    permissions: { contents: read, id-token: write }
    steps:
      - &ActCheckNocred { uses: *ActionsCheckout, with: *NoCreds }
      - &GoGomod { uses: *SetupGo, with: *GoMod }
      - *InstallShellCheck
      - &Uv { uses: *SetupUv, with: *NoCache }
      - &PyFlakes { name: Install pyflakes, run: *InstallPyflakes }

      - run: go test -v -race -coverprofile coverage.txt -covermode=atomic ./...
      - run: go tool cover -func ./coverage.txt

      # Set -race for catching data races on dog fooding (#333)
      - run: go build -race ./cmd/actionlint

      - name: Dog fooding 🐶
        # actionlint passes --norc to shellcheck, ignoring .shellcheckrc.
        run: echo "::add-matcher::.github/actionlint-matcher.json"; ./actionlint -color -shellcheck 'shellcheck -o all'

      - uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with: { use_oidc: true }
  wasm:
    name: Wasm
    runs-on: ubuntu-latest
    steps:
      - *ActCheckNocred
      - *GoGomod
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with: { node-version-file: .node-version, package-manager-cache: false }
      - &Runner { uses: kjanat/runner@8baa9ee978cfed038b98741749673c788ff161d1 } # v0.26.1
      - &Pandoc { name: Install Pandoc, uses: $/.github/actions/setup-pandoc }
      - name: Install JavaScript dependencies
        run: runner install --frozen
      - name: Build command manual
        run: run make:man
      - name: Build and test playground
        run: run --dir playground -s make:build make:test
      - name: Lint JavaScript workspaces
        run: run lint
  release-snapshot:
    name: Release snapshot
    runs-on: ubuntu-latest
    steps:
      - uses: *ActionsCheckout
        with: { fetch-depth: 0, persist-credentials: false }
      - parallel:
          - &Go { uses: *SetupGo, with: *GoMod }
          - *Pandoc
          - *Runner
          - uses: &SetupNode actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
            with: { node-version: "24", package-manager-cache: false }
      - name: Build JavaScript Action release files outside the checkout
        run: |
          npm ci --ignore-scripts
          tag="$(git describe --tags --abbrev=0 --match 'v[0-9]*.[0-9]*.[0-9]*' --exclude '*-*')"
          node scripts/build-action-release.mjs --out-dir "${RUNNER_TEMP}/actionlint-action" --version "${tag#v}"
      - run: run make:man/actionlint.1
      - name: Install syft for SBOM generation
        uses: anchore/sbom-action/download-syft@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26 # v0.24.2
      - uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with: { version: v2.18.0, args: release --snapshot --clean }
      - name: Smoke-test the linux/amd64 snapshot binary
        run: ./dist/linux_linux_amd64_v1/actionlint -version
  action-modules:
    name: Action modules
    runs-on: ubuntu-latest
    steps:
      - *ActCheckNocred
      - uses: *SetupGo
        with: { go-version: stable, check-latest: true }
      # Self-contained modules under a dot-directory, which the go tool skips
      # when expanding a ... pattern. Test each in place.
      - name: Test and vet each action module
        run: |
          set -euo pipefail
          for mod in .github/actions/*/go.mod; do
            dir="$(dirname "${mod}")"
            echo "::group::${dir}"
            (cd "${dir}" && go vet ./... && go test ./...)
            echo "::endgroup::"
          done
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - *ActCheckNocred
      - *Go
      - parallel:
          - *Uv
          - *InstallShellCheck
          - name: Install golangci-lint
            uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
            with: { version: v2.13.1, install-only: true }
          - *Runner
      - *PyFlakes
      - run: go mod tidy -diff
      - name: Build through Makefile
        run: run make:build
        env: &SkipGoGen { SKIP_GO_GENERATE: "true" }
      - run: run make:lint
        env: *SkipGoGen
      - name: Lint bash scripts
        run: shellcheck ./scripts/*.bash
  docker:
    name: Dockerfile
    runs-on: ubuntu-latest
    steps:
      - *ActCheckNocred
      - name: Build image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with: { load: true, push: false, tags: actionlint:test }
      - name: Test Docker image
        run: docker container run
          --mount type=bind,source="${PWD}",target=/w
          -- actionlint:test -color -verbose
      - name: Lint Dockerfile with hadolint
        run: docker run --rm -i hadolint/hadolint hadolint --ignore DL3018 --strict-labels
          --require-label org.opencontainers.image.source:url
          --require-label org.opencontainers.image.licenses:spdx
          - < Dockerfile
      - name: Lint Dockerfile with droast
        uses: immanuwell/dockerfile-roast@1c87b2aa189338c24db4acde40864dae8994de8f # 1.7.0
  action:
    name: GitHub Action
    strategy:
      fail-fast: false
      matrix: { os: [ubuntu-latest, macos-latest, windows-latest] }
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - uses: *ActionsCheckout
        with: { fetch-depth: 0, persist-credentials: false }
      - parallel:
          - *GoGomod
          - { uses: *SetupNode, with: { node-version: "24", package-manager-cache: false } }
      - name: Test the JavaScript runtime and release packaging
        run: |
          npm ci --ignore-scripts
          npm run typecheck --workspace actionlint-action
          npm test --workspace actionlint-action
          node --test scripts/build-action-release.test.mjs
      - name: Prepare the ordinary binary and release action
        env: { BINARY_SUFFIX: "${{ runner.os == 'Windows' && '.exe' || '' }}" }
        run: |
          binary="${RUNNER_TEMP}/actionlint-test${BINARY_SUFFIX}"
          go build -o "${binary}" ./cmd/actionlint
          echo "ACTIONLINT_ACTION_BINARY=${binary}" >> "${GITHUB_ENV}"
          tag="$(git describe --tags --abbrev=0 --match 'v[0-9]*.[0-9]*.[0-9]*' --exclude '*-*')"
          node scripts/build-action-release.mjs --out-dir "${RUNNER_TEMP}/actionlint-action" --version "${tag#v}"
          cp -R "${RUNNER_TEMP}/actionlint-action/action" .actionlint-action-test
      - name: Check Git-tag version fallback
        working-directory: packages/github-action
        run: |
          env -u ACTIONLINT_VERSION node --run build -- --out-dir "${RUNNER_TEMP}/actionlint-action-fallback"
          cmp "${RUNNER_TEMP}/actionlint-action-fallback/main.mjs" "${RUNNER_TEMP}/actionlint-action/action/dist/main.mjs"
      - name: Exercise the action through the real runner
        id: smoke
        # ./ loads the release bundle generated in this job's workspace.
        uses: ./.actionlint-action-test
        with: { files: testdata/ok/minimal.yaml, format: json }
      - name: Check runner outputs and input compatibility
        env:
          EXIT_CODE: ${{ steps.smoke.outputs.exit-code }}
          RESULT: ${{ steps.smoke.outputs.result }}
          PROBLEM_COUNT: ${{ steps.smoke.outputs.problem-count }}
          OUTPUT: ${{ steps.smoke.outputs.output }}
        run: |
          test "${EXIT_CODE}" = 0
          test "${RESULT}" = success
          test "${PROBLEM_COUNT}" = 0
          test "${OUTPUT}" = '[]'
          actionlint_path="$(command -v actionlint)"
          [[ "${actionlint_path}" == */actionlint-bin-*/actionlint* ]]
          actionlint -version
          shellcheck --version
          pyflakes --version
          node scripts/test-action.mjs "${RUNNER_TEMP}/actionlint-action/action" "${ACTIONLINT_ACTION_BINARY}"
      - name: Check exported tools from PowerShell
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          actionlint -version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          shellcheck --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          pyflakes --version
          exit $LASTEXITCODE
  action-result:
    name: GitHub Action
    needs: action
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Require all Action platforms to pass
        env: { RESULT: "${{ needs.action.result }}" }
        run: test "${RESULT}" = success
  pre-commit-shellcheck:
    name: pre-commit ShellCheck integration
    runs-on: ubuntu-latest
    steps:
      - *ActCheckNocred
      - parallel: [*Go, *Uv]
      - parallel:
          - run: uv tool install pre-commit
          - name: Remove the runner's ShellCheck so only the hook's own can be found
            run: sudo apt-get remove -y shellcheck
      - name: Confirm shellcheck is not on PATH
        run: if command -v shellcheck; then echo "shellcheck is still on PATH; test invalid" >&2; exit 1; fi
      - name: Confirm actionlint-shellcheck reports a fixture's SC2086
        run: |
          cat > .github/workflows/_pre-commit-shellcheck-fixture.yml <<'FIXTURE'
          name: CI
          on: push
          jobs: {fixture: {runs-on: ubuntu-latest, steps: [{run: "echo $GITHUB_SHA"}]}}
          FIXTURE
          set +e
          pre-commit try-repo . actionlint-shellcheck --files .github/workflows/_pre-commit-shellcheck-fixture.yml --verbose > pre-commit-output.txt 2>&1
          status=$?
          set -e
          cat pre-commit-output.txt
          rm .github/workflows/_pre-commit-shellcheck-fixture.yml
          test "${status}" -ne 0
          grep -q SC2086 pre-commit-output.txt
